{{>layouts/header}}

    <div class="container">
      <form method="post" action="/burgers/{{id}}">
        <div class="form-group row">
          <label>이름</label>
          <div class="col-sm-10">
            <input type="text" name="name" value="{{burger.name}}" class="form-control">
          </div>
        </div>
        <div class="form-group row">
          <label>가격</label>
          <div class="col-sm-10">
            <input type="text" name="price" value="{{burger.price}}" class="form-control">
          </div>
        </div>
        <div class="form-action">
          <button class="btn btn-primary">제출</button>

        </div>


      </form>

    </div>



  </body>
</html>